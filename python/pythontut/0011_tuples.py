coordinates = (4, 5) # coordinates[1] = 10 does not work, because tuples are immutable, they cannot be changed
print(coordinates[0])
#you can make a list of tuples

coords = [(9,4), (4, 6), (3,7)]
print(coords)
print(coords[2])
